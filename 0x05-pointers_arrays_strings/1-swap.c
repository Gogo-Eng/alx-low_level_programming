#include "main.h"
#include <stdio.h>

/**
 * swap_int -  a function that swaps the values of two integers.
 * @a: the first parameter passed to the function
 * @b: the second parameter passed to the function
 * Return: returns nothing
 */

void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
