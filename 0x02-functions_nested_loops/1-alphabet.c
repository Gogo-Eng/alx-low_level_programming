#include "main.h"

/**
 * print_alphabet - a function to make the alphabet
 * return(void)
 */
	void print_alphabet(void)

{
	char r;
	for (r = 'a'; r <= 'z'; r++) {
		_putchar(r);
	}
		_putchar('\n');

}
